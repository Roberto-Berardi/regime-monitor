# Tools

One-time build tooling, kept for reference.

`make_template.py` converted the approved HTML into the Jinja template in
`templates/`. It has already run; edit the template directly.

`page-final.html` is the approved design it was generated from.

The rest are one-off patch and audit scripts from the build.
